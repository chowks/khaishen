---
import Icon from 'astro-icon';

interface Props {
  icon: string;
}

const { icon } = Astro.props;
---

<div
  class="w-20 h-20 border-[0.25px] border-white border-solid flex items-center justify-center"
>
  <Icon name={icon} size="35" fill="#000000" stroke="#000000" />
</div>
